# FastAPI Geospatial Processing Service Environment Configuration
HOST=127.0.0.1
PORT=8000
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: Google Gemini API Key (Auxiliary plain-language municipal explainer)
# Note: Deterministic spatial calculation, 3D volume extrusion, and 3D ULPIN generation NEVER require an API key.
# GEMINI_API_KEY=
